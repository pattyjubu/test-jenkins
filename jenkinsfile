pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
			{env.BUILD_ID}
                echo 'Building..'
				def python_pth = ${env.WORKSPACE} + '/' + 'hello.py'
				sh 'python ' + python_pth
            }
        }
    }
}